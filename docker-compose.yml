services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: usine_a_site
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
